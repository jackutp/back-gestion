[build]
builder = "DOCKERFILE"
dockerfilePath = "microservicio-incidentes/Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
healthcheckPath = "/actuator/health"
healthcheckTimeout = 300
